{
  "routes": [
    {
      "method": "GET",
      "path": "/email-accounts",
      "handler": "email-account.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/email-accounts/count",
      "handler": "email-account.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/email-accounts/:id",
      "handler": "email-account.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/email-accounts",
      "handler": "email-account.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/email-accounts/:id",
      "handler": "email-account.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/email-accounts/:id",
      "handler": "email-account.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
